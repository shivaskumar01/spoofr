# Virtual environments — machine-specific, never commit
.venv/
.venv_old_tk9/
env/
venv/

# Python build & cache
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
*.spec
.pytest_cache/

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Local runtime / settings / logs
.spoofr/
*.log

# Note: website/Spoofr-macOS.zip (the ~72MB standalone download) IS committed
# on purpose, so Netlify serves it. PyInstaller build artifacts (build/, dist/,
# *.spec) above are not.

# Tooling locks not used by this project (setup is micromamba/conda — see README)
uv.lock

# Editors
.idea/
.vscode/
